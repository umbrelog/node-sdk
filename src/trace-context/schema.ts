/** Execution context propagation schema version (x-umbrelog-schema-version header). */
export const EXECUTION_CONTEXT_SCHEMA_VERSION = 1;

/** Default runtime interaction metadata schema version. */
export const DEFAULT_RUNTIME_INTERACTION_VERSION = 1;
